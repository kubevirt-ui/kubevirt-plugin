export enum QuotaColumn {
  ADDITIONAL = 'additional',
  CPU = 'cpu',
  CREATION_TIME = 'creationTime',
  MEMORY = 'memory',
  NAME = 'name',
  NAMESPACE = 'namespace',
  VM_COUNT = 'vmCount',
}

export enum QuotaScope {
  CLUSTER = 'cluster',
  PROJECT = 'project',
}
