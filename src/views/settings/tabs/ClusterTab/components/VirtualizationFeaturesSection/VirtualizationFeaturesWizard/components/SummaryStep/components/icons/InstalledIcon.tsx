import React, { FCC } from 'react';

import { CheckCircleIcon } from '@patternfly/react-icons';

import './virt-wizard-icons.scss';

const InstalledIcon: FCC = () => <CheckCircleIcon className="installed-icon" />;

export default InstalledIcon;
