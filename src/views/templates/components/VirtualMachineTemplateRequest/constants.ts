export enum VMTemplateRequestConditionType {
  Progressing = 'Progressing',
  Ready = 'Ready',
}

export enum VMTemplateRequestStatus {
  Failed = 'Failed',
  InProgress = 'InProgress',
  Succeeded = 'Succeeded',
}
