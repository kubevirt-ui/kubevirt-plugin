import { CSSProperties } from 'react';

export const popupbackgroundStyle: CSSProperties = {
  bottom: 0,
  left: 0,
  position: 'fixed',
  right: 0,
  top: 0,
  zIndex: 100,
};

export const treeViewActionMenuStyle = { transform: 'translateY(30px) translateX(50px)' };
