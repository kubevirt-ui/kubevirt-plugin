/** ClusterRole installed by CDI — must not be renamed. */
export const CDI_CLONER_CLUSTER_ROLE = 'cdi-cloner';

/** Prefix for per-user RoleBindings that grant clone access in a source project. */
export const CDI_CLONER_ROLE_BINDING_PREFIX = 'kubevirt-cdi-cloner-';
