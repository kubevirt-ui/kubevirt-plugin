---
description: Coverage Analyst agent — read-only Playwright E2E coverage assessment using repo source code as the single source of truth
alwaysApply: false
---

# Coverage Analyst

Invoked by: `/coverage-analysis [scope] [options]`

Performs a read-only assessment of Playwright E2E test coverage for the virtualization module. Uses two complementary methods:
1. **Automation ID cross-reference** — matches `data-test` attributes and `ouiaId` props in `src/` against Playwright locator usage
2. **Feature-surface enumeration** — decomposes `src/views/` routes into testable capabilities and maps them to spec files

**Options:**

| Option               | Default  | Description                                                                          |
| -------------------- | -------- | ------------------------------------------------------------------------------------ |
| `--enrich`           | off      | Extend with git commit history and Jira ticket data (requires VPN for Jira)          |
| `--type=features`    | features | (Only with `--enrich`) Ticket types: `features`, `bugs`, or `all`                    |
| `--since=3months`    | 3 months | (Only with `--enrich`) Commit history lookback                                       |
| `--compare=<branch>` | off      | Compare current branch coverage against another branch (e.g. `main`, `release-4.17`) |

**Hard constraints:**
- No code modifications, no branch creation, no test execution
- No checkout of other branches — `--compare` reads via `git ls-tree`/`git show` only
- Playwright tests only — Jest/unit tests are not considered coverage
- No external dependencies by default (works offline on any clone)
- Deterministic — same checkout yields identical results

**Excluded routes:**
- `networking` — Networking tests are owned by a separate team and excluded from all coverage analysis runs.

---

## UI Automation ID Convention

This project uses a **hybrid approach** for UI automation identifiers:

- **`ouiaId` prop** — used on OUIA-compliant PatternFly components (Button, Card, TextInput, Switch, Modal, Toolbar, Checkbox, Radio, Title, DropdownItem, Table, Tr, Menu, Alert, Select). PF renders `data-ouia-component-id` and `data-ouia-component-type` automatically.
- **`data-test` attribute** — used on plain HTML elements (span, div, a) and non-OUIA PF/custom components (MenuToggle, EmptyState, SelectOption, DescriptionItem, etc.).

In Playwright:
- `this.testId('id')` → matches `data-test` attributes (via `testIdAttribute: 'data-test'` in config)
- `this.ouia('id')` → matches `data-ouia-component-id` attributes (via CSS selector)

The automation ID cross-reference (Phase 1) must extract **both** attribute types from `src/` and **all** locator patterns from Playwright to produce accurate coverage data.

---

## Test Structure

Tests are organized across multiple tiers:

| Tier         | Directory                    | Purpose                                   | Status                       |
| ------------ | ---------------------------- | ----------------------------------------- | ---------------------------- |
| **Gating**   | `playwright/tests/gating/`   | Smoke: navigation, page loads, basic CRUD | Must always pass, no retries |
| **Tier 1**   | `playwright/tests/tier1/`    | Single-resource CRUD lifecycle            | Active                       |
| **Tier 2**   | `playwright/tests/tier2/`    | Cross-module integration                  | Active                       |
| **Settings** | `playwright/tests/settings/` | Cluster-wide config (runs in isolation)   | Active                       |
| **API**      | `playwright/tests/api/`      | API contract validation                   | Active                       |

Coverage analysis scans **all** directories. Proposals target the appropriate tier based on test complexity.

---

## Route Registry

Each route maps source code directories to test directories.

| Route ID | Source Path | Spec Keywords |
|----------|------------|---------------|
| `vms` | `src/views/virtualmachines/list/`, `tree/`, `navigator/` | `vm-list`, `virtual-machines`, `vm-tree` |
| `vm-detail` | `src/views/virtualmachines/details/` | `vm-overview`, `vm-tabs`, `vm-configuration` |
| `vm-actions` | `src/views/virtualmachines/actions/` | `vm-action`, `lifecycle`, `migrate` |
| `wizard` | `src/views/virtualmachines/wizard/` | `wizard`, `create-vm`, `creation-wizard` |
| `templates` | `src/views/templates/` | `templates`, `template-` |
| `bootable-volumes` | `src/views/bootablevolumes/` | `bootable-volume`, `boot-source` |
| `instance-types` | `src/views/instancetypes/` | `instance-type`, `instanceType` |
| `migration-policies` | `src/views/migrationpolicies/` | `migration-polic`, `migrationpolic` |
| `migrations` | `src/views/storagemigrations/` | `migration`, `vm-migration`, `storage-migration` |
| `checkups` | `src/views/checkups/` | `checkup`, `self-validation` |
| `settings` | `src/views/settings/` | `settings`, `cluster-settings` |
| `overview` | `src/views/clusteroverview/` | `overview`, `landing`, `dashboard` |

---

## Data Pipeline

### Default Mode (repo-only, no flags)

#### Phase 1: Automation ID Cross-Reference

This is the primary, code-level coverage signal.

##### Step 1a: Extract all automation IDs from `src/`

```bash
# Static data-test IDs
rg 'data-test="([^"]+)"' src/ --glob '*.tsx' --glob '*.ts' -o \
  | sed 's/.*data-test="//;s/"//' | sort -u > /tmp/src-data-test-ids.txt

# Static ouiaId values
rg 'ouiaId="([^"]+)"' src/ --glob '*.tsx' --glob '*.ts' -o \
  | sed 's/.*ouiaId="//;s/"//' | sort -u > /tmp/src-ouia-ids.txt

# Dynamic prefixes
rg '(?:data-test|ouiaId)=\{`([^$`]+)' src/ --glob '*.tsx' --glob '*.ts' -o \
  | sed 's/.*=\{`//' | sort -u > /tmp/src-ids-dynamic.txt

# Merge all
cat /tmp/src-data-test-ids.txt /tmp/src-ouia-ids.txt /tmp/src-ids-dynamic.txt \
  | sort -u > /tmp/src-ids-all.txt
```

##### Step 1b: Extract all automation ID references from Playwright

```bash
{
  # testId('id') — matches data-test attributes
  rg "testId\(" playwright/ --glob '*.ts' --glob '*.tsx' \
    | perl -nle "print \$1 if /testId\(['\"]([^'\"]+)['\"]/"

  # getByTestId('id') — matches data-test attributes
  rg "getByTestId\(" playwright/ --glob '*.ts' --glob '*.tsx' \
    | perl -nle "print \$1 if /getByTestId\(['\"]([^'\"]+)['\"]/"

  # ouia('id') — matches data-ouia-component-id attributes
  rg "ouia\(" playwright/ --glob '*.ts' --glob '*.tsx' \
    | perl -nle "print \$1 if /ouia\(['\"]([^'\"]+)['\"]/"

  # CSS attribute selectors
  rg '\[data-test="[^"]+"\]' playwright/ --glob '*.ts' --glob '*.tsx' -o \
    | grep -oP '(?<=\[data-test=")[^"]+'
  rg '\[data-ouia-component-id="[^"]+"\]' playwright/ --glob '*.ts' --glob '*.tsx' -o \
    | grep -oP '(?<=\[data-ouia-component-id=")[^"]+'
  rg '\[data-test-id="[^"]+"\]' playwright/ --glob '*.ts' --glob '*.tsx' -o \
    | grep -oP '(?<=\[data-test-id=")[^"]+'
} | sort -u > /tmp/pw-ids.txt
```

##### Step 1c: Cross-reference

For each ID in `src/`:
- **COVERED** — exact match (or dynamic prefix match) found in Playwright refs
- **UNCOVERED** — no Playwright reference targets this element

For each ID in Playwright:
- **IN SRC** — the ID exists in our `src/` code
- **EXTERNAL** — the ID comes from Console SDK, PatternFly, or other external dependencies (not actionable)

##### Step 1d: Map uncovered IDs to source files and feature areas

```bash
# For each uncovered ID, find ALL source files containing it
rg -l '(?:data-test|ouiaId).*"<ID>"' src/ --glob '*.tsx' --glob '*.ts'
```

#### Phase 2: Feature-Surface Enumeration

Enumerate testable capabilities from `src/views/` for each route in scope:

```bash
rg --files src/views/<route> --glob '*.tsx' | wc -l
```

Feature taxonomy per route:
- **List page**: renders, filters (name, row-filter), sort, column management, bulk actions, create button
- **Detail page**: each tab/sub-tab loads, inline edits, action menus
- **Actions**: each modal (delete, migrate, clone, stop, restart, etc.)
- **Forms/Wizards**: each step renders, validation, submission, review
- **CRUD**: create via YAML, create via form/wizard, edit, delete

#### Phase 3: Test Inventory (spec scanning)

Count spec files and tests per tier:

```bash
for tier in gating tier1 tier2 settings api; do
  files=$(rg --files playwright/tests/$tier --glob '*.spec.ts' | wc -l)
  tests=$(rg -c 'test\(' playwright/tests/$tier --glob '*.spec.ts' \
    | awk -F: '{s+=$2}END{print s+0}')
  echo "$tier: $files specs, $tests tests"
done

rg 'test\(' playwright/tests/ --glob '*.spec.ts' --no-heading -N
```

#### Phase 4: Coverage Mapping

Merge signals from Phase 1 (automation ID cross-reference) and Phase 2 (feature enumeration):

| Status       | Criteria                                                                                         |
| ------------ | ------------------------------------------------------------------------------------------------ |
| **TESTED**   | A Playwright spec explicitly exercises this capability. Automation ID cross-reference is supplemental evidence. |
| **PARTIAL**  | A spec exists for the route but doesn't exercise this specific sub-feature, or only some aspects covered. |
| **UNTESTED** | No Playwright spec touches this capability. Missing automation IDs reinforce the gap. |

#### Phase 5: Scoring

Per-route:
```
tested_count = TESTED + (PARTIAL * 0.5)
total = TESTED + PARTIAL + UNTESTED
score = (tested_count / total) * 100
```

#### Phase 6: Proposals

For each UNTESTED capability:
1. Find if an existing spec covers the route area
2. Generate proposal: EXTEND existing spec or NEW spec file
3. Target the appropriate tier:
   - Gating: navigation and page load smoke tests (rare — gating is mostly frozen)
   - Tier 1: single-resource CRUD lifecycle
   - Tier 2: cross-module integration
   - Settings: cluster-wide configuration changes (runs in isolation)
4. All proposals use `scenarioTest` fixture (or `gatingTest` for gating), `RequestContextClient` for K8s setup, and UI-based navigation (never `page.goto`)

---

### Enriched Mode (`--enrich` flag)

When `--enrich` is provided, add commit history and Jira ticket data **after** the base report:

#### Enrichment Step A: Commit Scan

```bash
git log --oneline --since="3 months ago" -- src/views/ | grep -oP 'CNV-\d+' | sort -u
```

Map commits to routes by path (same path registry as above).

#### Enrichment Step B: Jira Queries (requires VPN)

```bash
BASE_JQL='project = CNV AND component = "CNV User Interface"'
TYPE_FILTER='AND type IN (Story, Epic, "Feature Request")'
VERSION_FILTER='AND fixVersion IN ("CNV v4.22.0", "CNV v4.23.0")'
KEYWORD='AND text ~ "<route-jql-keywords>"'
```

#### Enrichment Step C: Annotation Check

```bash
rg "CNV-XXXXX" playwright/tests/ --type ts -l
```

Enriched data appears as an **addendum** section. The base coverage score is always repo-only.

### Compare Mode (`--compare=<branch>`)

When `--compare` is provided, re-run Phases 1-4 against the comparison branch using `git show` and `git grep` (never checkout), then compute deltas.

```bash
git rev-parse --verify <branch> 2>/dev/null

# Phase 1 on <branch>: extract automation IDs
git grep -h 'data-test=\|ouiaId=' <branch> -- 'src/**/*.tsx' 'src/**/*.ts' \
  | perl -nle 'print $1 if /(?:data-test|ouiaId)="([^"]+)"/' | sort -u > /tmp/compare-src-ids.txt

# Phase 1 on <branch>: extract Playwright refs
git grep -h 'testId\|getByTestId\|ouia\|data-test=\|data-ouia-component-id=' <branch> -- 'playwright/**/*.ts' \
  | perl -nle 'print $1 if /(?:testId|getByTestId|ouia)\(['\''"]([^'\''"]+)/' > /tmp/compare-pw-ids.txt

# Phase 2-3 on <branch>: feature and test inventory
git ls-tree -r --name-only <branch> -- src/views/ | sort
git ls-tree -r --name-only <branch> -- playwright/tests/ | sort
git show <branch>:playwright/tests/<path>
```

---

## Scoring Rubric

| Score Range | Rating | Meaning |
|-------------|--------|---------|
| 90-100% | Excellent | Near-complete coverage for the route |
| 70-89% | Good | Most features tested, minor gaps |
| 50-69% | Fair | Significant untested features |
| 30-49% | Poor | Majority of features lack coverage |
| 0-29% | Critical | Route has minimal or no Playwright coverage |

---

## Proposal Tier Assignment

| Criteria | Tier |
|----------|------|
| Page load smoke, navigation only | Gating (rare) |
| Single-resource CRUD, form interactions | Tier 1 |
| Cross-module workflows, multi-resource | Tier 2 |
| Cluster-wide settings changes | Settings |

---

## Output Formatting Rules

- Use markdown tables for structured data
- Bold the overall coverage percentage
- Sort per-route table by coverage ascending (worst first)
- Sort gap tables by feature impact (user-facing first)
- Limit proposals to top 10 highest-impact items
- Include both automation ID and feature-surface statistics in the summary
- No emojis in output
- When `--enrich` is used, clearly separate the addendum from the base report
- When `--compare` is used, append the comparison section after proposals

---

## Output Template

```markdown
## Coverage Analysis: Virtualization Module

**Source of truth:** Repository (current branch)
**Scope:** <scope> | **Method:** automation ID cross-reference + feature-surface enumeration
**Overall coverage:** XX% (YY/ZZ testable capabilities covered by Playwright tests)

### Automation ID Cross-Reference Summary

| Metric                                  | Count |
| --------------------------------------- | ----- |
| data-test IDs defined in src/           | NNN   |
| ouiaId values defined in src/           | NNN   |
| IDs referenced in Playwright            | NNN   |
| src/ IDs covered by tests               | NNN (XX%) |
| src/ IDs uncovered                      | NNN (XX%) |
| Playwright refs from external sources   | NNN |

### Uncovered Elements by Feature Area

| Feature Area        | Uncovered IDs | Element Types                     | Impact |
| ------------------- | ------------- | --------------------------------- | ------ |
| migrationpolicies   | 9             | List columns                      | HIGH   |
| storagemigrations   | 5             | List cells                        | HIGH   |
| clusteroverview     | 7             | Migrations table cells            | MEDIUM |
| ...                 | ...           | ...                               | ...    |

> **Note:** vmnetworks is excluded (owned by a separate team).

### Per-Route Breakdown

| Route           | Implemented Features | Tested | Partial | Untested | Coverage |
| --------------- | -------------------- | ------ | ------- | -------- | -------- |
| VMs (list/tree) | 12                   | 6      | 1       | 5        | 54%      |
| Wizard          | 14                   | 8      | 1       | 5        | 61%      |
| ...             | ...                  | ...    | ...     | ...      | ...      |

### Priority Gaps (ordered by impact)

| #   | Route     | Untested Feature | Uncovered IDs            | Proposal                           |
| --- | --------- | ---------------- | ------------------------ | ---------------------------------- |
| 1   | migpol    | List cell values | migration-policy-name... | EXTEND: tier1/migrationpolicies/...  |
| 2   | storagemig| All list cells   | storage-migration-name...| NEW: tier2/storagemigrations/...     |

### Summary Statistics

| Metric                      | Value |
| --------------------------- | ----- |
| Routes analyzed             | N     |
| Total testable capabilities | ZZ    |
| Capabilities tested         | YY    |
| Automation ID coverage      | XX%   |
| Gaps identified             | NN    |
| Proposals generated         | PP    |
| Spec files (gating)         | N     |
| Spec files (tier1)          | N     |
| Spec files (tier2)          | N     |
| Spec files (settings)       | N     |
| Spec files (api)            | N     |
```

---

## Scope Resolution

When the user provides a scope argument:

1. Look up the route ID in the Route Registry table above
2. If found, only analyze that single route
3. If not found, try fuzzy matching (e.g., `vm` → `vms`, `storage` → `migrations`)
4. If still ambiguous, list matching routes and ask user to clarify
5. If no scope or `all`, analyze all routes

---

## Integration with Other Commands

- After running `/coverage-analysis`, the user may invoke `/create-test` to implement proposals
- The proposals section should reference the exact spec file path that `/create-test` would target
- Each proposal must include: action (EXTEND/NEW), target file, target tier, and effort estimate
- All proposals use `scenarioTest`/`gatingTest` fixture and `RequestContextClient` for K8s setup
